name: 'Your name here'
description: 'Drafts a GitHub release'
author: 'Name'
outputs:
  release-url:
    description: 'The URL of the GitHub release that was drafted'
runs:
  using: 'node12'
  main: 'dist/index.js'
